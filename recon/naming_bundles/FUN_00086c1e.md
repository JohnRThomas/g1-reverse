### FUN_00086c1e  @ 0x00086c1e  (size=38)

callers (24): spec_ble_command_hook, ble_process_get_req, ble_process_put_req, ble_process_req_dispatch, subcontracing_send_data_pkcs7, flash_settings_write_and_verify, enqueue_file, try_to_save_file, esb_send_command_and_wait_ack, send_data_in_ble_chunks, DashBoard_Reflash, ui_even_ai_task, gui_utf_draw, gui_utf_draw_dark_light_split, gui_utf_draw_truncate, gui_utf_draw_middle, gui_utf_draw_align_right, gui_utf_Wordwrap_draw, proxy_thread_handler, gatt_dm_start, img_mgmt_upload, projector_send_command, FUN_0007e776, flash_write_padded_entry
callees (2): FUN_00051164, memcpy

```c
/* readable reconstruction; identity: FUN_00086c1e @ 0x00086c1e
 * public-name: FUN_00086c1e
 * durable-map: recon/catalogs/function_names_app.json
 * callees (readable <= raw @ address):
 *   memcpy                                   <= FUN_00086c04 @ 0x00086c04
 */
/* Reconstructed FUN_00086c1e @ 0x86c1e. */

#include <stdint.h>

extern void FUN_00051164(void) __attribute__((noreturn));
extern void memcpy(void *destination, const void *source,
                         unsigned int length);

void FUN_00086c1e(void *destination, const void *source,
                  unsigned int length, unsigned int destination_size)
{
    const unsigned char *src = source;
    unsigned char *dst = destination;
    uintptr_t src_address = (uintptr_t)src;
    uintptr_t dst_address = (uintptr_t)dst;

    if (length > destination_size ||
        (src_address <= dst_address &&
         dst_address < src_address + length) ||
        (dst_address <= src_address &&
         src_address < dst_address + length)) {
        FUN_00051164();
    }

    memcpy(dst, src, length);
}
```