/*
 * Cohesive translation unit for the `unsorted' module.
 *
 * GENERATED by recon/refactor/transforms/t04_cohesive_tu.py.  Do not
 * edit: a stage tree is regenerated, never hand-patched
 * (recon/refactor/README.md C4).
 *
 * The 3 functions below were 3 separate translation units until this
 * stage.  Each one's text is reproduced VERBATIM and in the order the
 * generated source list gave it, and they were CONSECUTIVE in that list.
 * They were proven able to share a translation unit: no symbol is
 * declared here with two types, no identifier has internal linkage in one
 * member and is named by another, and no local typedef, macro, struct tag
 * or enum constant of one member is visible to a member that names it.
 *
 * members, in link order:
 *   dispatch_via_object_index.c
 *   g1_clock_release_hfclk_checked.c
 *   array_slot_ptr_b098.c
 */
/* ---- dispatch_via_object_index.c ---- */
#include "g1_app_symbols.h"
#include <stdint.h>
#include "g1_unsorted.h"
/* readable reconstruction; identity: FUN_00060564 @ 0x00060564
 * public-name: dispatch_via_object_index
 * durable-map: recon/catalogs/function_names_app.json
 * callees (readable <= raw @ address):
 *   clock_control_nrf_on                     <= FUN_000604e4 @ 0x000604e4
 *   dispatch_via_object_index                <= FUN_00060564 @ 0x00060564
 * address symbols (name @ address):
 *   rodata_87b30                             @ 0x00087b30
 */
/* Reconstructed FUN_00060564 @ 0x60564  (parity: 300/300 trials, PROVEN) */

extern unsigned int clock_control_nrf_on(unsigned int,int,unsigned int);
typedef void (*fnptr)(int,unsigned int);
static void dispatch_via_object_index(int param_1, fnptr UNRECOVERED_JUMPTABLE)
{
  unsigned int uVar1;
  uVar1 = clock_control_nrf_on(((unsigned long)&rodata_87b30) /*=0x87b30*/,(param_1 - 0x2000b098) >> 5,0x40);
  (*UNRECOVERED_JUMPTABLE)(param_1,uVar1);
  return;
}

/* ---- g1_clock_release_hfclk_checked.c ---- */
/* readable reconstruction; identity: FUN_00060744 @ 0x00060744
 * public-name: g1_clock_release_hfclk_checked
 * durable-map: recon/catalogs/function_names_app.json
 * callees (readable <= raw @ address):
 *   g1_clock_release_hfclk_checked           <= FUN_00060744 @ 0x00060744
 *   nrfx_clock_stop                          <= FUN_00065324 @ 0x00065324
 */
/* Reconstructed FUN_00060744 @ 0x60744.
 * BASEPRI-guarded HFCLK release caller; raw/address back-map is preserved.
 */


static void g1_clock_release_hfclk_checked(void)
{
    uint32_t saved_basepri;
    __asm volatile("mrs %0, basepri" : "=r"(saved_basepri));
    __asm volatile("msr basepri_max, %0\n\tisb sy" :: "r"((uint32_t)0x20) : "memory");

    volatile uint32_t *state = (volatile uint32_t *)0x2000b148UL;
    uint32_t previous = *state;
    *state = previous & ~2u;
    if ((previous & 1u) == 0u) {
        nrfx_clock_stop(1);
    }

    __asm volatile("msr basepri, %0\n\tisb sy" :: "r"(saved_basepri) : "memory");
}

/* ---- array_slot_ptr_b098.c ---- */
/* readable reconstruction; identity: FUN_00060778 @ 0x00060778
 * public-name: array_slot_ptr_b098
 * durable-map: recon/catalogs/function_names_app.json
 * callees (readable <= raw @ address):
 *   array_slot_ptr_b098                      <= FUN_00060778 @ 0x00060778
 */
/* Reconstructed FUN_00060778 @ 0x60778  (parity: 300/300 trials, PROVEN) */

static unsigned int array_slot_ptr_b098(unsigned int param_1)
{
  unsigned int b = param_1 & 0xffu;
  return 0x2000b098UL + (b << 5);
}

