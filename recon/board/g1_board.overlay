/*
 * G1 app-core board overlay (draft) — nrf5340dk_nrf5340_cpuapp
 *
 * Reconstructed from app_update.bin device objects + pinctrl state tables.
 * Bus/pin mapping and provenance: recon/board/pins_buses.md
 *
 * nRF5340 SERIAL mapping in this firmware:
 *   UARTE0=uart@8000  TWIM1=i2c@9000  SPIM2/SPIM4=projector(runtime pins)
 *   TWIM3=i2c@b000    PDM0=pdm@26000  QSPI=mx25r6435f
 * No PWM node exists in the firmware (brightness is set over projector SPI).
 */

&pinctrl {
	/* ---- QSPI (external NOR flash) ---- */
	qspi_default: qspi_default {
		group1 {
			psels = <NRF_PSEL(QSPI_SCK, 0, 17)>,
				<NRF_PSEL(QSPI_IO0, 0, 13)>,
				<NRF_PSEL(QSPI_IO1, 0, 14)>,
				<NRF_PSEL(QSPI_IO2, 0, 15)>,
				<NRF_PSEL(QSPI_IO3, 0, 16)>,
				<NRF_PSEL(QSPI_CSN, 0, 18)>;
			nordic,drive-mode = <NRF_DRIVE_H0H1>;
		};
	};
	qspi_sleep: qspi_sleep {
		group1 {
			psels = <NRF_PSEL(QSPI_SCK, 0, 17)>,
				<NRF_PSEL(QSPI_IO0, 0, 13)>,
				<NRF_PSEL(QSPI_IO1, 0, 14)>,
				<NRF_PSEL(QSPI_IO2, 0, 15)>,
				<NRF_PSEL(QSPI_IO3, 0, 16)>,
				<NRF_PSEL(QSPI_CSN, 0, 18)>;
			low-power-enable;
		};
	};

	/* ---- I2C1 (i2c@9000): OPT3007 ALS + PMIC ---- */
	i2c1_default: i2c1_default {
		group1 {
			psels = <NRF_PSEL(TWIM_SDA, 0, 5)>,
				<NRF_PSEL(TWIM_SCL, 0, 4)>;
		};
	};
	i2c1_sleep: i2c1_sleep {
		group1 {
			psels = <NRF_PSEL(TWIM_SDA, 0, 5)>,
				<NRF_PSEL(TWIM_SCL, 0, 4)>;
			low-power-enable;
		};
	};

	/* ---- I2C3 (i2c@b000): LSM6DSO IMU ---- */
	i2c3_default: i2c3_default {
		group1 {
			psels = <NRF_PSEL(TWIM_SDA, 1, 2)>,
				<NRF_PSEL(TWIM_SCL, 1, 3)>;
		};
	};
	i2c3_sleep: i2c3_sleep {
		group1 {
			psels = <NRF_PSEL(TWIM_SDA, 1, 2)>,
				<NRF_PSEL(TWIM_SCL, 1, 3)>;
			low-power-enable;
		};
	};

	/* ---- UARTE0 (uart@8000): console/debug ---- */
	uart0_default: uart0_default {
		group1 {
			psels = <NRF_PSEL(UART_TX, 0, 20)>;
		};
		group2 {
			psels = <NRF_PSEL(UART_RX, 0, 22)>;
			bias-pull-up;
		};
		/* RTS/CTS map to P3.31 in-image => unrouted, hw-flow-control off */
	};
	uart0_sleep: uart0_sleep {
		group1 {
			psels = <NRF_PSEL(UART_TX, 0, 20)>,
				<NRF_PSEL(UART_RX, 0, 22)>;
			low-power-enable;
		};
	};

	/* ---- PDM0 (pdm@26000): microphone ---- */
	pdm0_default: pdm0_default {
		group1 {
			psels = <NRF_PSEL(PDM_CLK, 1, 13)>,
				<NRF_PSEL(PDM_DIN, 1, 14)>;
		};
	};

	/*
	 * ---- Projector control SPI (SPIM4 / SPIM2) ----
	 * Pins are assigned at runtime by spi_master_init (@0x26418) from the
	 * projector-controller struct; not fixed in the image. Fill SCK/MOSI/
	 * MISO/CS from a live board before enabling.
	 */
	spi4_default: spi4_default {
		group1 {
			/* TODO: psels = <NRF_PSEL(SPIM_SCK, x, y)>, ... ; */
		};
	};
	spi2_default: spi2_default {
		group1 {
			/* TODO: psels = <NRF_PSEL(SPIM_SCK, x, y)>, ... ; */
		};
	};
};

/* ================= peripheral nodes ================= */

&qspi {
	status = "okay";
	pinctrl-0 = <&qspi_default>;
	pinctrl-1 = <&qspi_sleep>;
	pinctrl-names = "default", "sleep";

	mx25r64: mx25r6435f@0 {
		compatible = "nordic,qspi-nor";
		reg = <0>;
		writeoc = "pp4io";
		readoc = "read4io";
		sck-frequency = <8000000>;
		jedec-id = [c2 28 17];
		size = <67108864>; /* 64 Mbit */
		has-dpd;
		t-enter-dpd = <10000>;
		t-exit-dpd = <35000>;
	};
};

&i2c1 { /* i2c@9000 */
	compatible = "nordic,nrf-twim";
	status = "okay";
	pinctrl-0 = <&i2c1_default>;
	pinctrl-1 = <&i2c1_sleep>;
	pinctrl-names = "default", "sleep";
	clock-frequency = <I2C_BITRATE_FAST>;

	opt3007: opt3001@45 {
		compatible = "ti,opt3001";
		reg = <0x45>;
	};

	pmic: pmic@6b {
		reg = <0x6b>;
		/* nPM PMIC; set compatible per part (e.g. nordic,npm1300) */
	};
};

&i2c3 { /* i2c@b000 */
	compatible = "nordic,nrf-twim";
	status = "okay";
	pinctrl-0 = <&i2c3_default>;
	pinctrl-1 = <&i2c3_sleep>;
	pinctrl-names = "default", "sleep";
	clock-frequency = <I2C_BITRATE_FAST>;

	lsm6dso: lsm6dso@6b {
		compatible = "st,lsm6dso";
		reg = <0x6b>;
		/* IMU; int line via GPIO if used */
	};
};

&uart0 { /* uart@8000 */
	status = "okay";
	current-speed = <115200>;
	pinctrl-0 = <&uart0_default>;
	pinctrl-1 = <&uart0_sleep>;
	pinctrl-names = "default", "sleep";
	/* hw-flow-control omitted: RTS/CTS not routed */
};

&pdm0 { /* pdm@26000 */
	status = "okay";
	pinctrl-0 = <&pdm0_default>;
	pinctrl-names = "default";
};

/*
 * Projector control SPI — enable once runtime pins are captured:
 *
 * &spi4 {
 *     status = "okay";
 *     pinctrl-0 = <&spi4_default>;
 *     pinctrl-names = "default";
 *     cs-gpios = <&gpio0 ... GPIO_ACTIVE_LOW>;
 * };
 * &spi2 { ... same pattern ... };
 */

/* ================= GPIO control lines / buttons ================= */
/ {
	gpio_keys {
		compatible = "gpio-keys";
		sw0: sw0 {
			gpios = <&gpio1 9 (GPIO_ACTIVE_LOW | GPIO_PULL_UP)>;
			label = "SW0";
		};
		sw1: sw1 {
			gpios = <&gpio1 10 (GPIO_ACTIVE_LOW | GPIO_PULL_UP)>;
			label = "SW1";
		};
	};

	panel_ctrl {
		compatible = "gpio-leds";
		panel_pwr: panel_pwr {
			gpios = <&gpio0 24 GPIO_ACTIVE_HIGH>;
			label = "panel-power-enable";
		};
		/* additional projector/panel control outputs observed at boot:
		 * P0.19, P0.21, P0.23, P0.30 (+ P0.25/26/27/28) */
	};
};
