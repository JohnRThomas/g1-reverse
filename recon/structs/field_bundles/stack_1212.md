### struct att_find_type_req_locals  (G1-original)  cid=stack_1212

purpose: Local fragment in att_find_type_req (local_38): an attribute-type byte plus a second byte.

This object is accessed by these functions (read their fully-named source):
  - att_find_type_req  (as local_38)  [no source file]

Current layout — fields named `field_0xNN` NEED names; keep offsets unchanged:
```c
struct att_find_type_req_locals {
    uint8_t      field_0x0;  /* +0x0  rw=r sz=1 */   <-- NAME ME
    uint8_t      field_0x2;  /* +0x2  rw=r sz=1 */   <-- NAME ME
};
```