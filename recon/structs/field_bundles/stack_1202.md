### struct att_read_group_req_locals  (G1-original)  cid=stack_1202

purpose: Stack-local ATT Read-By-Group-Type request scratch bytes in att_read_group_req.

This object is accessed by these functions (read their fully-named source):
  - att_read_group_req  (as local_44)  [no source file]

Current layout — fields named `field_0xNN` NEED names; keep offsets unchanged:
```c
struct att_read_group_req_locals {
    uint8_t      field_0x0;  /* +0x0  rw=r sz=1 */   <-- NAME ME
    uint8_t      field_0x2;  /* +0x2  rw=r sz=1 */   <-- NAME ME
};
```